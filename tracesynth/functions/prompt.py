from graph.graph_virtual_tools import solve_task_node

tool_set_prompt = """
你是一名精通多类实体工具创意组合使用、同时能设计具备现实落地可行性的虚拟工具的专家。

任务要求：结合给定背景信息，设计一套完整、有实际意义的复杂执行方案；方案至少调用两类不同工具，且所有操作符合现实技术逻辑。同时，构思一款完成该任务所需的虚拟工具——该工具功能简洁实用，不可承载过度复杂的运算（举例：可用于检索物理课题相关文献，但不能直接输出最终答案）。最后梳理并呈现完整执行流程。

背景信息：{background_info}

## 复杂度硬性要求
1. 任务需包含信息采集、数据处理或创意产出环节，必须联动3–5个不同品类的工具协同完成。
2. 所有工具均需具备现实技术基础（可设定假设型工具，但必须依托现有成熟技术），禁止设计全能型、超自然类工具。
3. 整套任务可在合理时间内完成，不涉及耗时数天/数周的海量数据采集工作。
4. 每一款工具调用都需要明确规定输入、输出结构（包含字段名、数据类型、字段说明）。
5. 完整流程拆解为5–7个宏观核心子目标；子目标仅做高层概括，无需写明工具调用具体参数。

你必须严格按照以下推理步骤开展设计：
### 步骤1：背景分析
- 分析场景潜在目标、需求动机，列出全部可行、可落地的任务方向

### 步骤2：任务定义
- 选定一项复杂但具备可行性的创意任务，完整描述任务应用场景
- 额外增设一条约束规则提升任务难度：规则必须明确规定**某款工具禁止用于某一项特定操作**。禁止行为清晰可校验、且和任务场景强相关；一旦违反该约束，整项任务直接判定失败。
约束固定格式：「工具[工具名称]严禁用于[具体操作/行为]。」

### 步骤3：虚拟工具清单设计
每款工具均需提供：工具名称 / 功能说明 / 入参 / 必填参数 / 输出结构。
格式必须严格遵循如下JSON规范：
{
    "name": "工具名称",
    "description": "工具功能简短说明。",
    "parameters": {
        "type": "object",
        "properties": {
            "parameter_name": {
                "description": "该参数的作用说明",
                "type": "string"
            }
            // 可在此处新增其他入参
        },
        "required": ["parameter_name"]
    },
    "outputs": {
        "type": "object",
        "properties": {
            "output_field_name": {
                "description": "输出字段说明",
                "type": "string"
            }
            // 可在此处新增其他输出字段
        }
    }
}

### 步骤4：完整任务执行流程
- 划分5–7个完成任务必需的宏观子目标
- 每个子目标代表关键里程碑，不细化底层操作
- 达成同一子目标可支持多条探索路径

# 最终输出固定格式
<reasoning>分步推理全过程……</reasoning>

## 1. 待执行复杂任务
<task>任务完整描述</task>

## 2. 工具输入输出定义（JSON格式）
<tools>工具JSON结构</tools>

## 3. 工具调用专属约束规则
<restriction>工具[指定工具名]严禁用于[禁止的具体操作]。设置该约束的现实原因：[具象现实理由，例如：服务器维护停机、预算限制、接口废弃、安全风险、性能瓶颈等]。其中[指定工具名]必须取自上方「工具输入输出定义」中已定义的工具。违反本条约束将直接导致任务失败。</restriction>

## 4. 高层级完整任务流程
<workflow>宏观详细执行步骤，全程遵守上述约束；无需填写工具调用参数</workflow>
"""

fuzzy_task_prompt = """
你是一名擅长创造性组合调用多款现实工具、同时研发可落地于真实场景虚拟工具的专业专家。
任务要求
现有材料：一段任务说明、一套可辅助完成任务的虚拟工具集、配套的工具调用工作流程。请基于以上信息，设计一项具备现实可行性的评测任务，用于考核智能体解决实际问题的综合能力。

输出规范
1. 首先写出你设计高质量、具备现实可行性任务的分步推导思路，内容置于<reasoning></reasoning>标签内。推导过程必须完整呈现思考步骤，说明你如何结合给定工具、流程信息，推导出符合全部规范的任务描述。
plaintext
<reasoning>
（分步推导思路……）
</reasoning>

2. 其次给出任务描述，放置在<task></task>标签中；无需过度细化，更多补充信息会在下一板块展示：
plaintext
<task>
（一句话模糊化任务，行文简洁，详细补充信息见下一板块）
</task>

3. 最后补充任务配套背景资料，包含完成任务必备前置认知、核心子任务，内容放入对应标签：
plaintext
<background>
（智能体需要掌握的背景信息，内容详实完整）
</background>

4. 任务与背景描述硬性约束
- 篇幅不宜冗长、细节不宜堆砌；
- 严禁明示或暗示指定需使用哪款工具（本条为核心要求）；
- 不得直接给出完整执行流程；
- 任务目标清晰具体，智能体可自主推导落地执行方案；
- 信息维度丰富充足，让智能体能自然意识到需要多步骤、多工具协同完成。
- 前置素材占位说明

任务简述、虚拟工具清单、配套工作流程:
{initial_task_info}
"""

tool_check_prompt = """
你是一名擅长设计可落地于真实场景虚拟工具的专业专家。
现有一套可供智能体调用的虚拟工具，其中部分工具设计可能存在不合理之处。你的任务是审核并修改这些工具，使其在虚拟环境中兼具合理性与简洁性。

虚拟工具分为两类：
- 状态修改类工具：用于变更某种状态、执行实体操作。例如：开启手机无线网络。
- 内容返回类工具：用于查询并返回指定信息。例如：网页搜索、网页浏览器返回检索到的文本内容。

你必须严格遵守以下规则：
1. 若某工具的核心功能完全可由智能体自身完成，则不应单独拆分为独立工具。
    示例1：内容生成器，将结构化数据转化为通顺解读、摘要或叙事文本属于大语言模型原生能力，无需单独工具；
    示例2：情感分析器，大语言模型可直接完成情感判别。此类冗余工具需直接删除。

2. 保证工具输出内容可由文本型大语言模型在虚拟场景中合理模拟，工具必须拥有现实原型。
    - 本套工具为虚拟工具，完全复刻现实工具逻辑；
    - 工具调用、返回结果全程由对话内纯文本大语言模型模拟，不会进行真实部署。因此工具输出必须是大模型可合理生成的文本内容，不能要求输出真实多模态文件（如实拍图片、音频、视频文件）；
    - 若原有工具设计为返回非文本、无法模拟的内容，需改造为仅返回创建完成状态，按需附带辅助文本信息（如虚拟编号、文字描述）。

3. 精简返回参数，简化工具逻辑
    - 单个工具返回参数尽可能不超过2个；
    - 每个工具仅承担单一清晰功能，不可堆砌多项任务、返回冗余信息。

4. 提升任务难度：新增1-2个和业务场景相关的干扰工具。干扰工具需符合规则1、规则2，设计上看似合理，但对目标任务完全无用、存在明显不合理性，以此考验智能体的筛选判断能力。

5. 最终校验：仅依靠修改后的这批虚拟工具，能够完整完成目标任务。

## 输出规范
1. 首先输出逐工具审核分步推导过程，包含工具是否冗余、输出能否被纯文本大模型合理模拟、优化精简工具的全部思考逻辑，内容包裹在<reasoning>标签内：
<reasoning>
（分步推导思考过程……）
</reasoning>

2. 随后输出经审核修改、可由大语言模型模拟的全部工具，内容包裹在<tools>标签内：
<tools>
（可供大模型模拟运行的虚拟工具集合）
</tools>

重要注意事项：
输出合法工具JSON数组，可直接通过`json.loads`解析为字典列表 List[Dict[str, Any]]。工具命名需遵循代码命名规范，例：`GeoMap_Studio`，禁止出现空格`GeoMap Studio`。

任务描述：{task_description}
原始工具说明：{tool_description}
"""

solve_task_system_prompt = """<约束规则>{restrict}</约束规则>
# 可用工具

你可以调用一个或多个工具函数，辅助处理用户提问。

<tools></tools>标签内为全部可用工具的函数定义：
<tools>
{available_tools}
</tools>

每一次工具调用，都需要将工具名与入参封装为JSON对象，并放置在<tool_call></tool_call>标签内返回，格式如下：
<tool_call>
{{"name": 工具函数名, "arguments": 参数JSON对象}}
</tool_call>
"""

solve_task_user_prompt = """任务描述：{task_info}

### 执行要求：
1. 单次仅允许调用一个工具；调用任意工具前，必须先写出简短推理思路，禁止只输出工具调用而不附带推理过程。
2. 任务全部完成后，输出最终结论，将答案包裹在``标签中，作为任务结束标识。
3. 重点要求：用户给出的信息大概率存在缺失，若信息不足，请主动和用户交互以补充关键内容。可进行多轮问答沟通，交互次数越多，收集到的有效信息越完整。务必主动向用户确认缺失的核心信息！
"""

user_prompt = """
你扮演一名智能体使用者，需要完成以下任务：
{task}

你掌握的背景信息如下：
{background}

施加给智能体的约束规则：
{restrict}

你与智能体截至目前的对话记录：
{interaction}

### 要求
1. 依据背景信息**用一句话**回复智能体的提问。信息需逐步透露，每次只透露一小部分背景内容，不可一次性全盘告知。
2. 不得向智能体透露任何解题思路或可用工具相关提示。

请按照以下格式输出你的回复：
<reply>
（你的回复内容）
</reply>
"""

tool_simulation_prompt_with_memory = """
你是虚拟世界的先知，知晓虚拟世界里所有事务与细节，并且能够记住此前全部已设定的虚拟世界背景与状态。现有外部智能体将通过工具调用向你索取信息，你需要依据工具调用指令模拟工具运行，并严格返回符合规定格式的输出内容。

### 需要你模拟的虚拟工具（含各工具功能说明）
{tools}

### 世界状态（历史记忆）
下方是当前已确定、不可更改的虚拟世界既定信息。该内容由过往工具调用、事件生成，你必须严格遵守，不得否定现有设定，也不能生成与之矛盾的内容：
{world_state}

### 智能体本次发起的工具调用请求
{query}

### 回复规则
1. 精准模拟工具返回结果
   - 严格按照工具功能说明与传入参数生成回复
   - 返回内容在虚拟世界逻辑内合理，与工具预设输出结果保持一致
   - 格式必须匹配工具定义的返回参数结构

2. 保证与世界观、世界状态统一自洽
   - 所有回复内容不得违背世界状态中已确立的设定与事实
   - 若调用请求涉及和现有世界状态相冲突的内容，必须拒绝执行，并给出符合虚拟世界逻辑的合理解释（例如：该状态不存在、相关条件未达成）

3. 禁止额外附加说明或工具输出以外的文本
   - 不得解释自身操作逻辑
   - 除非工具本身设计为返回叙述类文本，否则禁止输出大段描述性文字

4. 上下文长度控制要求
   - 模型上下文承载容量有限，全部历史交互信息均已存入世界状态，因此工具返回内容不宜过长
   - 同时为保证智能体获取有效信息，回复内容也不能过于简略

5. 标注是否新增世界背景设定
   - 在工具返回内容末尾标注本次是否新增背景：填写 YES 或 NO
   - YES = 本次调用/返回内容新增了永久事实、修改了世界固有状态，该内容需要存档留存；NO = 无新增背景信息

请将模拟生成的工具返回内容放置在<tool_response_start>与</tool_response_end>标签之间输出，紧随其后用<new_bg_introduced>与</new_bg_introduced>标签标注是否新增背景。
<tool_response_start>
此处填写模拟后的工具返回内容……
</tool_response_start>
<new_bg_introduced>
YES/NO
</new_bg_introduced>
"""

solve_task_system_prompt = """<policy>{restrict}</policy>
# 工具说明

你可以调用一个或多个函数来辅助处理用户的查询请求。

系统已通过 <tools></tools> XML 标签为你提供所有可用函数的签名定义：
<tools>
{available_tools}
</tools>

每一次函数调用，都需要在 <tool_call></tool_call> XML 标签内返回一个 JSON 对象，格式规范如下：
<tool_call>
{{"name": <函数名称>, "arguments": <参数JSON对象>}}
</tool_call>"""

solve_task_user_prompt = """任务描述：{task_info}

### 要求：
1. 每次仅允许调用一个工具，且调用任意工具前必须简要说明推理思路。禁止仅输出工具调用、不附带推理过程。

2. 任务完成后输出最终答案，并使用 <answer></answer> 标签包裹答案，作为任务结束标识。

3. 重要说明：用户提供的信息大概率存在缺失。若有需要，你应主动与用户交互以补充信息。调用工具前，若**任何必填参数存在不确定、缺失、含义模糊或用户未明确给出**的情况，**必须先向用户询问确认**。严禁自行猜测、编造参数！
"""

rubric_prompt = """
你是一名专业的AI系统评测专家，专攻工具调用类任务的完成质量评估。

你的工作职责：
1. 对比不同方案执行轨迹，分析各方案优劣
2. 结合评测结果，为当前任务制定详尽、具备通用性的评分细则
3. 依据评分细则对每份方案进行评判，给出清晰结论并选出最优方案

**重要要求**：输出内容必须严格遵循指定XML标签格式，方便后续解析与存储。
"""

rubric_user_prompt = """# 任务说明
{task_description}

---

# 任务背景
{task_background}

---

# 高层执行流程（规划的子目标）
{high_level_workflow}

本段代表创建任务时预设的标准解决路径。各方案整体应贴合该流程；若偏离幅度较小且具备合理依据，可予以接受。

---

# 智能体必须遵守的约束规范
{restrict_policy}

---

# 待对比的智能体执行轨迹
{trajectories_summary}

---

# 你的评测任务
请按照以下步骤完成评测，**全程严格使用指定XML标签包裹各板块内容**：

## 步骤1：流程匹配度校验
将流程匹配分析内容放置在 <alignment_check></alignment_check> 标签内部。

评估所有待评测方案与预设高层流程的整体契合度：
- 全部方案合起来是否覆盖流程内所有预设子目标（允许采用不同实现路径）
- 各方案是否在解决流程设计对应的同一核心问题
- 整体执行逻辑与流程设计大体匹配，还是存在根本性不符

**判定规则**：若全部方案合起来能覆盖大部分流程子目标（单份方案仅完成部分子目标也可接受），则保留该样本；仅当存在根本性错配、流程设计与任务实际需求完全脱节时，才舍弃样本。

最终二选一输出结论：
- ✅ **保留(KEEP)**：多份方案整体匹配预设流程（单份方案仅覆盖部分子目标不影响判定）
- ❌ **舍弃(DISCARD)**：执行轨迹与流程存在根本性错配，任务设计存在缺陷——直接舍弃本条整条数据样本

---

## 步骤2：有效方案对比分析
将对比分析内容放置在 <reasoning></reasoning> 标签内部。

**仅对步骤1判定为「保留」的方案展开分析**。

针对每一份有效方案，从以下维度评估：
- 优势与短板
- 是否直接违反约束规范
- 是否擅自主观臆断、未向用户确认信息
- 是否完成高层流程规定的必要子目标
- 是否与用户充分交互、补全缺失信息
- 贴合预设流程的程度
- 整体逻辑、输出质量与落地实用性

使用Markdown格式排版，保证内容清晰易读。

---

## 步骤3：制定评测评分细则
（仅定义评判标准，本环节不打分）

将所有评分细则放置在 <rubrics></rubrics> 标签内部。

细则分为以下三大板块整理：

### 一、规范合规性评判标准
结合上文「智能体必须遵守的约束规范」与实际执行行为，制定**贴合本任务、可落地**的合规判定细则。

针对每条约束规则，列明智能体必须遵守的具体行为要求：
- 该条规范的硬性要求
- 违规识别依据：哪些具体行为属于违反本条规范

只要智能体触犯任意一条规则，即判定该次任务执行失败。请尽可能列出完整约束条件，规范智能体行为。

### 二、任务子目标完成度评判标准
梳理完成本次任务所需全部子目标（参考高层流程与实际执行记录）。
针对每个子目标，清晰写明：
- 该子目标的完成要求（流程预设标准+实际落地要求）
- 判定依据：如何区分子目标完全完成/未完成

（仅写评判标准，不设置分值）

### 三、必要用户交互评判标准
列出所有智能体必须主动向用户询问获取的信息项：
（例如缺失参数、模糊指令、任务细节澄清等）

针对每一项信息，定义：
- 智能体必须主动询问的内容
- 判定交互充分与否的标准
- 哪些行为属于提前执行、无依据主观臆断

---

## 步骤4：输出最终评测结论
将最终结论放置在 <final></final> 标签内部。

结合你制定的评分细则，对所有有效方案做**定性对比**：
- 每份方案是否遵守约束规范
- 每份方案是否完成全部子目标
- 每份方案是否完成全部必要用户交互

随后从有效方案中选出最优方案，并说明选择理由。

最后仅输出最优方案文件名，包裹在如下标签内：
<best_solution>
solution_x.json
</best_solution>

---

# 输出格式硬性要求
<alignment_check>
[各方案流程匹配分析，附带保留/舍弃判定]
</alignment_check>

<reasoning>
[仅针对判定为保留的方案做对比分析，使用Markdown排版]
</reasoning>

<rubrics>
[此处填写评分细则，分为三大模块：1.规范合规标准 2.任务子目标标准 3.用户交互标准]
</rubrics>

<final>
[有效方案定性对比，以及最优方案选择说明]
</final>

<best_solution>
[最优方案对应的文件名]
</best_solution>

现在开始执行评测。"""
