tool_set_prompt = """你是一名精通 Agentic RAG（智能体检索增强生成）系统架构设计的专家，擅长将 Query 优化、多路召回、检索后优化、相关性评估等环节抽象为可编排的工具（Tool），设计符合工业界落地标准的 Agentic RAG 数据合成场景。

## Agentic RAG 标准六步流程（设计时必须对齐）
1. **用户 Query 输入**：接收用户原始检索/问答请求。
2. **检索前优化**：根据 Query 特征，选择并调用 1 个或多个 Query_Optimization_Tools（如 Query 重写、Query 扩展、假设文档嵌入 HyDE、Query 分解、Query 后退提示 Step-back 等），输出优化后的 Queries。
3. **检索**：并行调用召回类工具（如 BM25 稀疏检索、向量语义检索、知识图谱查询、混合检索等），获取候选文档片段。
4. **检索后优化**：对召回结果去重、加权合并、对候选片段 Rerank 重排、上下文提炼（Refine）、结构化上下文内容等操作。
5. **相关性评估与迭代**：调用评估类工具，判断当前上下文是否足以回答 Query；若信息缺失，输出缺口分析与优化建议，**返回步骤 2** 发起新一轮 Query 优化与检索，直至信息完备。
6. **输出最终结果**：基于充分检索到的上下文，生成最终回答。

> 核心原则：除 step1 和 step6 外，step2~step5 每一步的具体实现方法均被抽象为独立工具；智能体通过工具编排完成全流程，而非单次检索即作答。

---

任务要求：结合给定背景信息，设计一套针对特定知识库场景的 Agentic RAG 执行方案。方案须覆盖全部 4 类 RAG 工具、对齐 step2~step5 四个必经步骤，共设计 {num_tools} RAG 工具（其中 {num_custom_tools} 为自定义虚拟组件）。同时针对该场景设计自定义 RAG 组件——功能聚焦单一（如：领域实体链接、时效性过滤、表格片段结构化提取），不可直接生成最终答案或替代完整检索链路。

## 本次合成复杂度参数
{complexity_summary}

背景信息：{background_info}

## 复杂度与技术要求
1. 工具须按流水线阶段分类设计，覆盖以下 4 类（step2~step5 每步至少 1 个工具）：
   - **Query_Optimization_Tools**（step2 检索前优化）：Query 重写 / 扩展 / HyDE / 分解 / Step-back 等
   - **Retrieval_Tools**（step3 检索）：BM25 / 向量检索 / 知识图谱 / 混合检索等
   - **Post_Retrieval_Tools**（step4 检索后优化）：去重 / 加权融合 / Rerank / 压缩去噪 / 结构化等
   - **Evaluation_Tools**（step5 相关性评估）：检索相关性评估、信息完备性判断、缺口诊断与优化建议
2. 所有工具须基于现实技术栈（Elasticsearch、Milvus/Pinecone、Neo4j、Cross-Encoder 等），禁止设计全知全能黑盒组件。
3. 每款工具须明确规定输入、输出结构（字段名、数据类型、字段说明）。
4. 完整流程须体现**步骤 5 的迭代回路**：{iteration_requirement}
5. 流程拆解为与六步流程对应的宏观子阶段；子目标仅做高层概括，无需写明超参数。

你必须严格按照以下推理步骤开展设计：

### 步骤1：知识库场景与检索挑战分析
- 分析背景下的知识库数据特征（非结构化文本、半结构化表格、图谱关联等）与用户 Query 动机。
- 识别 Agentic RAG 面临的痛点（语义断层、多跳推理、专有名词未对齐、信息分散需多轮补检等），说明为何需要迭代式检索而非单次召回。

### 步骤2：Agentic RAG 任务定义与边界约束
- 选定一项复杂但可行的 RAG 问答/检索任务（如跨文档多跳事实核验、含时效约束的对比分析等），完整描述应用场景与用户 Query 形态。
- 增设一条约束规则：明确规定**某款 RAG 工具严禁用于某项特定操作**。禁止行为须清晰可校验、与场景强相关。
- 约束固定格式：「工具[工具名称]严禁用于[具体操作/行为]。」

### 步骤3：RAG 工具清单设计
包含标准流水线工具及为该场景定制的虚拟组件。须设计 **{num_tools}** 工具（含 **{num_custom_tools}** 自定义组件），每款工具须标注所属阶段（检索前优化 / 检索 / 检索后优化 / 相关性评估）。格式严格遵循：
{{
    "name": "工具名称(英文命名)",
    "description": "工具功能说明，须标明所属 Agentic RAG 流水线阶段。",
    "parameters": {{
        "type": "object",
        "properties": {{
            "parameter_name": {{
                "description": "该输入参数的作用说明",
                "type": "string"
            }}
        }},
        "required": ["parameter_name"]
    }},
    "outputs": {{
        "type": "object",
        "properties": {{
            "output_field_name": {{
                "description": "输出字段及格式说明",
                "type": "string"
            }}
        }}
    }}
}}

### 步骤4：Agentic RAG 管道执行流设计
- 按六步流程划分宏观子阶段，明确 step2~step5 各阶段调用的工具类别与数据流向。
- **必须描述步骤 5 的迭代逻辑**：{iteration_requirement}
- 全程遵守步骤 2 中的约束条件。

# 最终输出固定格式
<reasoning>
分步推理全过程……
</reasoning>

## 1. 待执行的 Agentic RAG 任务
<task>用户 Query 场景及知识库背景完整描述（含为何需要多轮迭代检索）</task>

## 2. RAG 工具定义（JSON 数组格式）
<tools>RAG 工具 JSON 数组，每项工具 description 中须标注所属流水线阶段</tools>

## 3. 工具调用专属约束规则
<restriction>工具[指定工具名]严禁用于[禁止的具体操作]。设置该约束的现实原因：[具象理由，如精确率损失、索引延迟、图谱覆盖不足、计算开销等]。[指定工具名]必须取自上方工具定义。违反本条约束将直接导致任务失败。</restriction>

## 4. 高层级 Agentic RAG 管道流程
<workflow>按六步流程描述的宏观执行步骤，须包含步骤5迭代回路说明；无需填写工具调用具体参数</workflow>"""

fuzzy_task_prompt = """
你是一名 Agentic RAG 评测任务设计专家，擅长构造用于考核智能体检索增强问答能力的真实场景任务。

## 背景：Agentic RAG 标准流程
智能体须按以下流水线处理用户 Query（你设计的任务应能自然触发该流程，但**不得在任务描述中明示**）：
1. 接收用户 Query → 2. 检索前优化 → 3. 检索 → 4. 检索后优化（融合/Rerank/精炼）→ 5. 相关性评估（不足则迭代返回步骤2）→ 6. 输出最终答案

## 任务要求
现有材料：一段 Agentic RAG 任务说明、一套虚拟 RAG 工具集、配套的管道执行流程。请基于以上信息，设计一项具备现实可行性的评测任务，用于考核智能体能否通过工具编排完成复杂检索问答。

## 本次合成复杂度参数
{complexity_summary}

输出规范

1. 首先写出设计高质量评测任务的分步推导思路，置于 <reasoning></reasoning> 标签内。须说明：用户 Query 为何需要多轮迭代检索、背景知识如何支撑知识库模拟、任务如何隐式要求 Query 优化与多路召回协同。
<reasoning>
（分步推导思路……）
</reasoning>

2. 给出用户 Query（任务描述），置于 <task></task> 标签中。以**用户口吻**提出检索/问答请求，行文简洁，详细补充信息见下一板块：
<task>
（一句话模糊化用户 Query，模拟真实用户提问，不暴露 RAG 流水线细节）
</task>

3. 补充任务配套背景资料，置于 <background></background> 标签。内容须包含：
   - 知识库领域背景（智能体模拟检索时的「虚拟知识库」上下文）
   - 完成任务所需的前置认知与隐含子目标
   - 信息缺口设计：{iteration_requirement}（首轮检索不应一次凑齐全部答案要素）
<background>
（智能体需要掌握的背景信息，内容详实完整）
</background>

4. 任务与背景描述硬性约束
- 篇幅不宜冗长、细节不宜堆砌；
- 严禁明示或暗示须使用哪款 RAG 工具或执行哪一步流水线（本条为核心要求）；
- 不得直接给出 Agentic RAG 六步执行流程；
- 用户 Query 目标清晰具体，智能体可自主推导须通过检索工具获取信息；
- 信息维度丰富，让智能体自然意识到需要 Query 优化、多路召回、评估迭代等多步骤协同。

任务简述、虚拟 RAG 工具清单、配套管道流程:
{initial_task_info}
"""

tool_check_prompt = """
你是一名 Agentic RAG 虚拟工具设计审核专家，负责审核并优化供智能体调用的 RAG 流水线虚拟工具集。

## Agentic RAG 工具分类（审核时须对照，对应 step2~step5）
- **Query_Optimization_Tools**（step2 检索前优化）：Query 重写、扩展、HyDE、分解、Step-back 等
- **Retrieval_Tools**（step3 检索）：BM25、向量检索、知识图谱查询、混合检索等
- **Post_Retrieval_Tools**（step4 检索后优化）：去重、加权融合、Rerank、压缩去噪、结构化等
- **Evaluation_Tools**（step5 相关性评估）：检索相关性评估、信息完备性判断、缺口诊断

虚拟工具分为两类：
- **状态修改类**：变更索引状态、缓存查询计划等（较少见，须谨慎保留）。
- **内容返回类**：执行检索/优化/后处理/评估并返回文本结果（RAG 工具主体）。

你必须严格遵守以下规则：

1. 若某工具的核心功能完全可由智能体自身完成，则不应单独拆分为独立工具。
   - 示例1：**最终答案生成器**——基于已精炼上下文生成回答属于大模型原生能力，无需独立工具；
   - 示例2：**通用文本摘要器**——若仅做简单摘要且无独立检索语义，属冗余工具，应删除或合并入检索后优化类工具。
   - 保留：**检索相关性评估器**——虽可用 LLM 完成，但作为流水线显式步骤的工具应保留，因其输出（缺口诊断、是否迭代）驱动步骤 5→2 的回路。

2. 保证工具输出可由文本型大语言模型在虚拟场景中合理模拟，工具须有现实 RAG 技术原型。
   - 工具调用与返回全程由对话内纯文本大模型模拟，不会真实部署；
   - 返回内容须为文本（检索片段列表、排序分数、优化后 Query、评估报告等），不可要求真实多模态文件；
   - 检索类工具返回的文档片段须可在虚拟知识库逻辑内自洽生成。

3. 精简返回参数，简化工具逻辑
   - 单个工具返回参数尽可能不超过 2 个；
   - 每款工具仅承担单一流水线职责，不可堆砌多功能。

4. 提升任务难度：新增 **{distractor_tools}** 与 RAG 场景相关但对该任务无用的干扰工具（如：与任务领域无关的检索索引、错误阶段的工具）。干扰工具须看似合理，实则对当前 Query 全流程无帮助。

5. 最终校验：仅依靠修改后的工具集，智能体须能完整走通 Agentic RAG 六步流程（{iteration_requirement}）。

## 输出规范
1. 逐工具审核推导过程，包裹在 <reasoning> 标签内：
<reasoning>
（分步推导思考过程……）
</reasoning>

2. 经审核修改后的全部工具，包裹在 <tools> 标签内：
<tools>
（可供大模型模拟运行的虚拟 RAG 工具 JSON 数组）
</tools>

重要注意事项：
输出合法工具 JSON 数组，可直接通过 `json.loads` 解析为 `List[Dict[str, Any]]`。工具命名遵循代码规范，例：`HyDE_Query_Generator`，禁止空格。

任务描述（用户 Query）：{task_description}
原始工具说明：{tool_description}
"""

mock_user_prompt = """
你扮演一名向 Agentic RAG 系统提交检索/问答请求的真实用户，需要解决以下问题：
{task}

你掌握的背景信息如下（这些信息代表你作为提问者的领域认知，**不是**智能体可直接访问的知识库内容）：
{background}

施加给智能体的约束规则：
{restrict}

你与智能体截至目前的对话记录：
{interaction}

### 要求
1. 依据背景信息**用一句话**回复智能体的提问。信息须逐步透露，每次只透露一小部分，不可一次性全盘告知。
2. 你仅回答关于 Query 意图澄清、业务上下文、时间范围、实体指代等问题；**不得**替智能体提供检索结果或知识库文档内容。
3. 不得向智能体透露任何 RAG 流水线思路、工具名称或检索策略提示。

请按照以下格式输出你的回复：
<reply>
（你的回复内容）
</reply>
"""

tool_simulation_prompt_with_memory = """
你是虚拟知识库世界的先知，负责模拟 Agentic RAG 流水线中各工具的调用结果。你知晓虚拟知识库的全部文档、实体关系与索引状态，并能记住此前工具调用确立的检索上下文。

## 本次合成迭代复杂度
{iteration_requirement}

### 需要你模拟的虚拟 RAG 工具（含各工具功能说明）
{tools}

### 虚拟知识库状态（历史记忆）
下方是当前已确定、不可更改的虚拟知识库既定信息（含已召回文档、索引片段、图谱事实等）。该内容由过往工具调用生成，你必须严格遵守，不得否定现有设定，也不能生成矛盾内容：
{world_state}

### 智能体本次发起的工具调用请求
{query}

### 隐藏金标（仅供模拟检索结果对齐，勿在工具返回中直接泄露完整标准答案）
标准答案：{label}
参考上下文：{context}

### 回复规则
1. 精准模拟 RAG 工具返回结果
   - 严格按工具所属流水线阶段生成合理输出：
     - Query 优化类 → 返回优化后的 Query 列表及策略说明
     - 召回类 → 返回带 doc_id、片段文本、初始分数的候选文档列表
     - 检索后优化类 → 返回去重/融合/重排/精炼后的文档列表或结构化上下文
     - 评估类 → 返回相关性判定、信息缺口描述、是否建议迭代（及优化建议）
   - 返回格式须匹配工具定义的输出参数结构

2. 保证与虚拟知识库自洽
   - 检索结果须来自或符合虚拟知识库中的事实，不可凭空捏造与 world_state 矛盾的文档
   - 若请求参数与知识库状态冲突，拒绝执行并给出合理说明

3. 支持迭代检索场景
   - 按迭代复杂度要求模拟：{iteration_requirement}
   - 首轮召回可故意遗漏部分关键信息，促使智能体在评估阶段发现问题并发起补检
   - 当智能体按评估建议优化 Query 后再次检索，应返回补充性的相关片段；最多支持 {max_iterations} 轮有效补检

4. 禁止额外附加说明
   - 不得解释自身模拟逻辑，仅输出工具规定格式的结果

5. 上下文长度控制
   - 单次返回的文档片段总数与单段长度须适中：足够支撑推理，但避免过长

6. 标注是否新增知识库设定
   - 在工具返回末尾标注本次是否新增永久事实：YES 或 NO
   - YES = 新增文档/实体/关系须存档；NO = 仅返回已有知识的检索视图

请将模拟生成的工具返回内容放置在 <tool_response> 与 </tool_response> 标签之间，紧随其后用 <new_bg_introduced> 与 </new_bg_introduced> 标注是否新增背景。
<tool_response>
此处填写模拟后的工具返回内容……
</tool_response>
<new_bg_introduced>
YES/NO
</new_bg_introduced>
"""

solve_task_system_prompt = """<policy>{restrict}</policy>

# 角色
你是一名 Agentic RAG 编排智能体，负责通过调用 RAG 流水线工具处理用户 Query，完成检索增强问答。

# Agentic RAG 标准六步流程（须按序推进，步骤 5 可触发迭代）
1. **接收 Query**：理解用户检索/问答意图。
2. **检索前优化**：根据 Query 特征选择 Query_Optimization_Tools（重写/扩展/HyDE/分解/Step-back 等），输出优化 Queries。
3. **检索**：并行调用 Retrieval_Tools（BM25/向量/图谱/混合检索等）。
4. **检索后优化**：调用后处理工具完成去重、加权融合、Rerank、压缩去噪、结构化上下文。
5. **相关性评估**：调用评估工具判断上下文是否足以回答 Query；若信息缺失，根据建议**返回步骤 2** 发起新一轮优化与检索。
6. **输出最终答案**：信息完备后，基于检索上下文生成回答。

# 工具说明
系统已通过 <tools></tools> XML 标签提供所有可用 RAG 工具签名：
<tools>
{available_tools}
</tools>

每一次工具调用，须在 <tool_call></tool_call> XML 标签内返回 JSON：
<tool_call>
{{"name": <函数名称>, "arguments": <参数JSON对象>}}
</tool_call>"""

solve_task_user_prompt = """用户 Query：{task_info}

## 本次任务复杂度要求
{complexity_summary}

### 要求
1. 按 Agentic RAG 六步流程编排工具调用。每次仅允许调用一个工具，调用前须简要说明：当前处于哪一步、为何选择该工具、期望获得什么信息。

2. **迭代检索**：{iteration_requirement} 执行步骤 5 后，若评估工具判定信息不足，须根据缺口建议返回步骤 2 优化 Query 并重新检索，直至上下文足以回答用户 Query（至少 {min_iterations} 轮、至多 {max_iterations_val} 轮迭代补检，若上限为 0 则无需迭代）。

3. 信息完备后输出最终答案，用 <answer></answer> 标签包裹，作为任务结束标识。答案须基于工具检索到的上下文，不得凭空编造知识库中不存在的事实。

4. 用户提供的信息可能存在缺失。调用工具前，若**任何必填参数不确定、缺失或含义模糊**，**须先向用户询问确认**，严禁自行猜测或编造参数。

5. 严格遵守 <policy> 中的工具调用约束，违规将导致任务失败。
"""

rubric_prompt = """
你是一名专业的 Agentic RAG 系统评测专家，专攻 RAG 流水线工具调用与检索问答任务的完成质量评估。

你的工作职责：
1. 对比不同方案的执行轨迹，分析各方案在 Agentic RAG 六步流程上的优劣
2. 结合评测结果，为当前任务制定详尽、具备通用性的评分细则
3. 依据评分细则对每份方案进行评判，给出清晰结论并选出最优方案

**重要要求**：输出内容必须严格遵循指定 XML 标签格式，方便后续解析与存储。
"""

rubric_user_prompt = """# 任务说明（用户 Query）
{task_description}

---

# 任务背景（虚拟知识库上下文）
{task_background}

---

# 高层 Agentic RAG 管道流程（预设子目标）
{high_level_workflow}

本段代表创建任务时预设的标准 Agentic RAG 解决路径，对应六步流程：
Query 输入 → 检索前优化 → 检索 → 检索后优化 → 相关性评估（可迭代）→ 最终输出。
各方案整体应贴合该流程；若偏离幅度较小且具备合理依据，可予以接受。

---

# 智能体必须遵守的约束规范
{restrict_policy}

---

# 待对比的智能体执行轨迹
{trajectories_summary}

---

# 你的评测任务
请按照以下步骤完成评测，**全程严格使用指定 XML 标签包裹各板块内容**：

## 步骤1：流程匹配度校验
将流程匹配分析内容放置在 <alignment_check></alignment_check> 标签内部。

评估所有待评测方案与预设 Agentic RAG 流程的整体契合度：
- 是否覆盖检索前优化、检索、检索后优化、相关性评估等关键阶段
- 是否在信息不足时正确触发步骤 5→2 的迭代回路（而非单次检索即作答）
- 各方案是否围绕同一用户 Query 与知识库场景展开
- 整体执行逻辑与预设流程大体匹配，还是存在根本性跳步或错序

**判定规则**：若全部方案合起来能覆盖大部分流程子目标（单份方案仅完成部分子目标也可接受），则保留该样本；仅当存在根本性错配（如完全跳过 Query 优化与评估迭代、流程与任务需求完全脱节）时，才舍弃样本。

最终二选一输出结论：
- ✅ **保留(KEEP)**：多份方案整体匹配预设 Agentic RAG 流程
- ❌ **舍弃(DISCARD)**：执行轨迹与流程存在根本性错配——直接舍弃本条数据样本

---

## 步骤2：有效方案对比分析
将对比分析内容放置在 <reasoning></reasoning> 标签内部。

**仅对步骤1判定为「保留」的方案展开分析**。

针对每一份有效方案，从以下维度评估：
- Agentic RAG 各阶段执行质量（Query 优化策略是否合理、多路召回是否充分、后处理是否到位）
- 迭代检索表现（是否在评估缺口后正确返回 Query 优化并补检）
- 是否直接违反约束规范
- 是否擅自主观臆断、未向用户确认模糊参数
- 最终答案是否基于检索上下文、有无幻觉
- 是否与用户充分交互
- 整体逻辑、输出质量与落地实用性

使用 Markdown 格式排版，保证内容清晰易读。

---

## 步骤3：制定评测评分细则
（仅定义评判标准，本环节不打分）

将所有评分细则放置在 <rubrics></rubrics> 标签内部。

细则分为以下四大板块整理：

### 一、规范合规性评判标准
结合「智能体必须遵守的约束规范」与实际执行行为，制定贴合本任务的合规判定细则。
针对每条约束，列明硬性要求与违规识别依据。触犯任意一条即判定任务失败。

### 二、Agentic RAG 流水线完成度评判标准
按六步流程梳理子目标，针对每个子目标写明：
- 完成要求（流程预设标准 + 实际落地要求）
- 判定依据：如何区分完全完成 / 未完成 / 跳步

重点关注：
- 检索前优化是否因 Query 特征选择合适策略
- 检索与检索后优化（融合/重排/精炼）是否被执行
- 相关性评估与迭代补检是否正确触发

### 三、检索质量与答案忠实度评判标准
- 最终答案是否可由检索上下文支撑
- 是否存在无依据编造（幻觉）
- 迭代前后检索结果是否形成有效信息补全

### 四、必要用户交互评判标准
列出智能体须主动向用户询问的信息项（Query 歧义、时间范围、实体指代等）。
针对每一项定义：须询问的内容、交互充分性标准、哪些行为属于无依据臆断。

---

## 步骤4：输出最终评测结论
将最终结论放置在 <final></final> 标签内部。

结合评分细则，对所有有效方案做**定性对比**：
- 规范合规性、流水线完成度、检索质量、用户交互

随后选出最优方案并说明理由。

最后仅输出最优方案文件名：
<best_solution>
solution_x.json
</best_solution>

---

# 输出格式硬性要求
<alignment_check>
[各方案流程匹配分析，附带保留/舍弃判定]
</alignment_check>

<reasoning>
[仅针对判定为保留的方案做对比分析，使用 Markdown 排版]
</reasoning>

<rubrics>
[评分细则四大模块：1.规范合规 2.流水线完成度 3.检索质量与忠实度 4.用户交互]
</rubrics>

<final>
[有效方案定性对比，以及最优方案选择说明]
</final>

<best_solution>
[最优方案对应的文件名]
</best_solution>

现在开始执行评测。"""
